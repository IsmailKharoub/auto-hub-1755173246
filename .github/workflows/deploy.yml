name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder deploy
        run: echo "Add your deploy steps here (e.g., Heroku, Render, Vercel)"
